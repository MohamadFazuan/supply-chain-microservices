spring:
  application:
    name: api-gateway
  config:
    import: configserver:http://localhost:8888
  cloud:
    config:
      username: config-user
      password: config-pass

bootstrap:
  context:
    enabled: true
